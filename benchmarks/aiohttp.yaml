project:
  name: aiohttp
  language: python
runtimes:
  python: ">=3.9,<3.13"
dependencies:
  python:
    - "aiohttp>=3.9"
entrypoint: "python -c \"import aiohttp; print(aiohttp.__version__)\""
test: "python -m pytest tests/ -x -q --no-header"
