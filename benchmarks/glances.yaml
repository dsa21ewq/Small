project:
  name: glances
  language: python
runtimes:
  python: ">=3.10,<3.13"
requirements_file: "requirements.txt"
dependencies:
  python:
    - "pytest>=7.0"
post_install:
  - "pip install ."
entrypoint: "glances --version"
test: "python -m pytest tests/ -x -q"
